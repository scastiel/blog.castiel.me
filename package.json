{
  "scripts": {
    "clean": "rm -rf dist/*",
    "generate": "miblog dist",
    "start": "http-server dist -p 3000"
  },
  "dependencies": {
    "http-server": "^0.9.0",
    "miblog": "^2.2.0"
  }
}
